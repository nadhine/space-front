function protocoloManager(obj)
	if(obj.protocolo == "destroyEnemy")
		-- executar funçoes de destruir inimigos
	end	

	if(obj.protocolo == "positionPlayer")
		-- executar funçoes de colocar player na posicao correta
	end
	if(obj.protocolo == "positionPlayerBullet")
		-- executar funçoes de mostrar balas da outra nave
	end
end